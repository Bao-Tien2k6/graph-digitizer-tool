scikit-image>=0.21.0
scikit-learn>=1.3.0
scipy>=1.11.0
Pillow>=10.0.0
numpy==1.26.4
# Single OpenCV build only: installing multiple cv2 distributions in one
# environment is unsupported and causes import shadowing / segfaults. The
# headless contrib build is a superset of the base package and needs no GUI libs.
opencv-contrib-python-headless==4.9.0.80
# paddleocr needs its engine (paddlepaddle); it is not pulled in automatically.
paddleocr==2.8.1
paddlepaddle==2.6.2
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
openpyxl>=3.1.0
matplotlib>=3.7.0
python==3.12.6
cv2
uvicorn

